sonar.projectKey=Learnathon-By-Geeky-Solutions_bladerunners
sonar.organization=learnathon-by-geeky-solutions
sonar.projectName=bladerunners
sonar.projectVersion=1.0

sonar.scanner.forceFullAnalysis=true

# Define the source directories for the backend (Laravel) and frontend (Next.js)
sonar.sources=backend

# Exclude directories and specific files
sonar.exclusions=backend/database/migrations/**,frontend,**/globals.css,**/index.php

sonar.security.exclusions=backend/app/Http/Middleware/**,frontend

# The ignore multicriteria below will only suppress issues but not remove them from metrics.
# If you want to disable any issue reporting (without excluding from analysis entirely),
# you can keep this section. Otherwise, rely on sonar.exclusions.
sonar.issue.ignore.multicriteria=e1,e2
sonar.issue.ignore.multicriteria.e1.ruleKey=*
sonar.issue.ignore.multicriteria.e1.resourceKey=**/globals.css
sonar.issue.ignore.multicriteria.e2.ruleKey=*
sonar.issue.ignore.multicriteria.e2.resourceKey=**/index.php

sonar.language=php
